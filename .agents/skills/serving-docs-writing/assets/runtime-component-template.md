# `<运行时组件名称>`

<!-- 用一句话说明该组件在 Serving 调用链中的职责。 -->

## 模块定位

说明组件的上游调用者、下游依赖、负责内容和明确边界。

## 代码组成

| 类型/函数 | 文件 | 职责 |
|---|---|---|
| `<symbol>` | [`<file>`](../../<file>) | `<responsibility>` |

只列理解组件所需的接口，不机械枚举私有函数。

## 核心接口

用表格说明相邻组件实际依赖的构造参数、方法、返回值和状态约束。

## 执行流程

按真实调用顺序说明主路径。prefill/decode、首次/warm 或正常/异常路径不同时分别描述。

## 数据位置与生命周期

| 数据/资源 | 表示 | 位置 | 所有者 | 生命周期与释放 |
|---|---|---|---|---|
| `<value>` | `<type>` | `<Host/NPU/disk>` | `<owner>` | `<lifetime>` |

## 缓存、驻留与复用

说明 cache key、命中条件、复用粒度和关闭行为。不存在缓存时删除本节，不要把普通 buffer reuse 称为 cache。

## 约束与错误处理

说明 shape、dtype、状态机、目录格式、对象归属和异常清理规则。

## 性能与可观测性

说明与该组件有关的 profile 事件、统计量和主要数据搬运。没有独立观测项时删除本节。

## 验证方法

### Host 侧验证

```bash
pytest -q <tests/serving/test_target.py>
```

### NPU 集成验证

```bash
<current hardware command>
```

只记录方法与预期不变量，不记录某次 PASS/FAIL 或耗时。

## 代码索引

| 内容 | 位置 |
|---|---|
| 实现 | [`<file>`](../../<file>) |
| 调用方 | [`<caller>`](../../<caller>) |
| 测试 | [`<test>`](../../<test>) |

## 相关文档

- [`Serving 总览`](README.md)
- `<上游或下游文档>`
