# `<入口或工作流名称>`

<!-- 用一句话说明用户为什么以及何时使用该入口。 -->

## 功能定位

说明入口解决的问题、适用场景，以及与其他 Serving 入口的区别。

## 前置条件

列出实际需要的环境、checkpoint、输入文件、expert cache 或其他产物。链接到公共说明，避免重复。

## 基本用法

给出当前代码可直接接受的最小命令，并解释输出。

## 命令行参数

| 参数 | 类型/默认值 | 作用与约束 |
|---|---|---|
| `<arg>` | `<type/default>` | `<behavior>` |

## 执行流程

按实际代码顺序解释从参数解析、资源创建、主要工作到资源关闭的流程。

## 输入与输出

说明 prompt、token、tensor、标准输出或磁盘文件的格式和约束。没有磁盘产物时删除相应内容。

## 资源与性能选项

说明该入口会创建和关闭的 runtime/loader，以及 profile、staging 或平台参数的真实作用。不适用时删除本节。

## 错误处理

列出用户能够触发的重要校验和异常清理保证。

## 验证方法

### CLI 单元测试

```bash
pytest -q <tests/cli/test_target.py>
```

### NPU 验证

```bash
<current hardware command>
```

只记录方法与预期不变量，不记录某次 PASS/FAIL 或耗时。

## 代码索引

| 内容 | 位置 |
|---|---|
| 入口 | [`<file>`](../../<file>) |
| 测试 | [`<test>`](../../<test>) |

## 相关文档

- [`Serving 总览`](README.md)
- `<相邻工作流或实现文档>`
